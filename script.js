document.addEventListener('DOMContentLoaded',()=>{const t=document.querySelector('.menu-toggle');const n=document.querySelector('.nav');if(t&&n)t.addEventListener('click',()=>n.classList.toggle('open'));document.querySelectorAll('[data-brand]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-brand]').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const b=btn.dataset.brand;document.querySelectorAll('.product-card').forEach(card=>card.style.display=(b==='all'||card.dataset.brand===b)?'flex':'none')}));const sel=document.querySelector('#interest');const qs=new URLSearchParams(location.search);if(sel&&qs.get('model'))sel.value=qs.get('model');});
